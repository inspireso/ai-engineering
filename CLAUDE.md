# AI Engineering 项目规范

## 意图级约束

编写 command（`commands/*.md`）和 skill（`skills/*/SKILL.md`）时，采用**意图级约束**风格：

- 各步骤描述**做什么**（目标与约束），而非**怎么做**（具体命令细节）
- 大模型已有的通用知识不在此赘述（如 git 命令、标准构建命令、格式模板等）
- 保留：项目特定的非标准操作、安全护栏、领域约定
